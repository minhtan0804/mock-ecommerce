export const validateMessages = {
  required: '${name} is required!',
  number: {
    max: '${name} has  8 to 20 character !',
    min: '${name} has  8 to 20 character !'
  },
  pattern: {
    mismatch: '${label} not invalid'
  }
}
